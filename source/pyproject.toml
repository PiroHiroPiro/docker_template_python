[project]
name = "source"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "notebook>=7.4.7",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "black>=25.9.0",
]
